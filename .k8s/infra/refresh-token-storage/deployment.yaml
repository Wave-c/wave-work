apiVersion: apps/v1
kind: Deployment
metadata:
  name: refresh-token-storage
  namespace: infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: refresh-token-storage
  template:
    metadata:
      labels:
        app: refresh-token-storage
    spec:
      containers:
      - name: refresh-token-storage
        image: redis:7-alpine
        args:
          - "--appendonly"
          - "yes"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 6379
