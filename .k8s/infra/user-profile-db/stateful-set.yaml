apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-profile-db
  namespace: infra
spec:
  selector:
    matchLabels:
      app: user-profile-db
  serviceName: user-profile-db
  replicas: 1
  template:
    metadata:
      labels:
        app: user-profile-db
    spec:
      securityContext:
        fsGroup: 999
      containers:
      - name: user-profile-db
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
          - name: PGDATA
            value: /var/lib/postgresql/data/pgdata
          - name: POSTGRES_DB
            value: user_profile_db
          - name: POSTGRES_USER
            value: wave
          - name: POSTGRES_PASSWORD
            value: Den1234567890
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 10Gi
