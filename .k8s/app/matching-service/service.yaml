apiVersion: v1
kind: Service
metadata:
  name: recomendation-service
  namespace: app
spec:
  selector:
    app: recomendation-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8077
  type: ClusterIP