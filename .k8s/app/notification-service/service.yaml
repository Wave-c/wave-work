apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: app
spec:
  selector:
    app: gateway-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8077
  type: ClusterIP