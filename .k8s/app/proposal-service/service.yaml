apiVersion: v1
kind: Service
metadata:
  name: proposal-service
  namespace: app
spec:
  selector:
    app: proposal-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP