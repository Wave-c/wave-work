events {}

http {
    # server {
    #     listen 80;

    #     location /api/bot/ {
    #         proxy_pass http://192.168.1.10:8052/;
    #     }

    #     location /api/subscription/ {
    #         proxy_pass http://192.168.1.10:8053/;
    #     }

    #     location /api/payment/ {
    #         proxy_pass http://192.168.1.10:8054/;
    #     }

    #     location /api/referral/ {
    #         if ($request_method = OPTIONS) {
    #             add_header 'Access-Control-Allow-Origin' 'https://f17bf49676bd.ngrok-free.app' always; 
    #             # http://192.168.1.10:3000
    #             add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
    #             add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type' always;
    #             add_header 'Access-Control-Allow-Credentials' 'true' always;
    #             return 204;
    #         }

    #         add_header 'Access-Control-Allow-Origin' 'https://f17bf49676bd.ngrok-free.app' always;
    #         # http://192.168.1.10:3000
    #         add_header 'Access-Control-Allow-Credentials' 'true' always;

    #         proxy_pass http://192.168.1.10:8055/;

    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_set_header Cookie $http_cookie;
    #     }

    #     location / {
    #         proxy_pass http://192.168.1.10:3000;
    #         proxy_http_version 1.1;

    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto https;

    #         # WebSocket / HMR (важно для dev)
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #     }
    # }

    server {
        listen 443 ssl;

        ssl_certificate /etc/nginx/ssl/selfsigned.crt;
        ssl_certificate_key /etc/nginx/ssl/selfsigned.key;

        # ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        location /api/referral/ {
            if ($request_method = OPTIONS) {
                add_header 'Access-Control-Allow-Origin' 'http://192.168.1.10:3000' always; 
                add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
                add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type' always;
                add_header 'Access-Control-Allow-Credentials' 'true' always;
                return 204;
            }

            add_header 'Access-Control-Allow-Origin' 'http://192.168.1.10:3000' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;

            proxy_pass http://192.168.1.10:8080/;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Cookie $http_cookie;
        }

        location / {
            proxy_pass http://192.168.1.10:3000;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;

            # WebSocket / HMR (важно для dev)
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}